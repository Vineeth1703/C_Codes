#include<iostream>
#include<stack>
using namespace std;
struct node{
    int data;
    node *next;
    node(int val):data(val),next(nullptr){}
};

int main()
{
    node* head=new node(1);
    node* head=new node(2);
    node* head=new node(3);
}